const evenAddNumbers = (num)=>{
    let sum=0;

    num.forEach(element) => {
        element% 2 == 0 && ()
        });
        return sum=num+num;
     
    };
    console.log(`cost for fencing rate Rs.${evenAddd([10,15,10,10,10,10,22,5,10)}/-`)